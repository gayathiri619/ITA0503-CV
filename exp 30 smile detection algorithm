import cv2

def detect_smile(image_path):

    # Load face detector
    face_cascade = cv2.CascadeClassifier(
        cv2.data.haarcascades +
        "haarcascade_frontalface_default.xml"
    )

    # Load smile detector
    smile_cascade = cv2.CascadeClassifier(
        cv2.data.haarcascades +
        "haarcascade_smile.xml"
    )

    # Read image
    image = cv2.imread(image_path)

    if image is None:
        print("Image not found! Check the image path.")
        return

    # Convert image to grayscale
    gray = cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)

    # Detect faces
    faces = face_cascade.detectMultiScale(
        gray,
        scaleFactor=1.1,
        minNeighbors=5,
        minSize=(30, 30)
    )

    # Detect smile inside each face
    for (x, y, w, h) in faces:

        face_roi = gray[y:y+h, x:x+w]

        smiles = smile_cascade.detectMultiScale(
            face_roi,
            scaleFactor=1.8,
            minNeighbors=20,
            minSize=(25, 25)
        )

        # Draw rectangle around smile
        for (sx, sy, sw, sh) in smiles:

            # Only consider lower half of face
            if sy > h // 2:

                cv2.rectangle(
                    image,
                    (x + sx, y + sy),
                    (x + sx + sw, y + sy + sh),
                    (0, 255, 0),
                    2
                )

                cv2.putText(
                    image,
                    "Smile",
                    (x + sx, y + sy - 5),
                    cv2.FONT_HERSHEY_SIMPLEX,
                    0.6,
                    (0, 255, 0),
                    2
                )

    print("Faces detected:", len(faces))

    # Display result
    cv2.imshow("Smile Detection", image)
    cv2.waitKey(0)
    cv2.destroyAllWindows()


# YOUR IMAGE PATH
detect_smile(r"C:\Users\GAYATHIRI\OneDrive\Pictures\Screenshots\Screenshot 2026-08-01 141848.png")
